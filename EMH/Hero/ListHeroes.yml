info:
  name: ListHeroes
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}emh.v1.HeroService/ListHeroes"
  body:
    type: json
    data: |-
      {
        "pagination": {
          "pageSize": 10
        },
        "searchQuery": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
