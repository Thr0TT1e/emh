info:
  name: ListSubmissions
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}emh.v1.SubmissionAdminService/ListSubmissions"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "pagination": {
          "pageSize": 20
        },
        "status": "PUBLICATION_STATUS_DRAFT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
